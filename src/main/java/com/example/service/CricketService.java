// package com.example.service;


    
// import java.util.List;

// import org.springframework.stereotype.Service;
// import com.example.dto.CricDTO;
// import com.example.model.Cricket;
// import com.example.repository.CricRepository;
// @Service
// public class CricketService {
//         private CricRepository repository;
    
//         public CricketService(CricRepository repository) {
//             this.repository = repository;
//         }
    
//         public int save(CricDTO dto) {
            // Cricket cricket = new Cricket();
            // cricket.setId(dto.getId());
            // cricket.setName(dto.getName());
            // cricket.setCountry(dto.getCountry());
            // cricket.setRuns(dto.getRuns());
            // return repository.save(cricket);
        // }
    
        // public List<Cricket> findAll() {
        //     return repository.findAll();
        // }
    
        // public Cricket findById(int id) {
        //     return repository.findById(id);
        // }
    
        // public int update(CricDTO dto) {
        //     Cricket cricket = new Cricket();
        //     cricket.setId(dto.getId());
        //     cricket.setName(dto.getName());
        //     cricket.setCountry(dto.getCountry());
        //     cricket.setRuns(dto.getRuns());
        //     return repository.update(cricket);
        // }
//}
